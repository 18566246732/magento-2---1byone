首次安装时，运行install[ubuntu].sh,以创建Python虚拟环境并安装依赖以及脚本。
安装需求： src下有正确的requirements.txt文件
安装出现异常：建议打开查看install[ubuntu].sh,按步骤找相应的问题和解决方案。
    第一步：安装virtualenv软件
    第二步：以系统为基础创建虚拟环境文件夹
    第三步：激活虚拟环境
    第四步：安装依赖
    第五步：安装脚本

安装完成后，以后可以直接调用run.sh运行。
用法：
输入query --help查看查询选项
输入deactivate退出虚拟环境

管理员维护：
如果安装新的模块，修改此文档